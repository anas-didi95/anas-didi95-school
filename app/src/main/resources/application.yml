---
datasources:
  CN_USER:
    password: ""
    url: "jdbc:h2:mem:DB_SCHOOL_USER;LOCK_TIMEOUT=10000;DB_CLOSE_ON_EXIT=FALSE"
    username: "sa"

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: "update"

logger:
  levels:
    com.anasdidi.school: "TRACE"
micronaut:
  application:
    name: "school"
